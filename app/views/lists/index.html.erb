<h1>All lists:</h1>


<table class="table">
  <thead>
    <tr>
      <th>Name</th>
    </tr>
  </thead>

</table>
<ul>
  <% @lists.each do |list| %>
    <li><%= link_to list.name, list_path(list) %></li>
  <% end %>
</ul>


<%= link_to "New list", new_list_path, class: 'btn btn-primary' %>
